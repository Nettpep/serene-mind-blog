# คู่มือรองรับ Traffic สูง (หลักล้านคน)

## 📊 สถานะปัจจุบัน: รองรับได้แค่ไหน?

| สถานะ | รองรับได้ประมาณ | หมายเหตุ |
|--------|------------------|----------|
| **ก่อนเพิ่ม cache** | หลักร้อย–พันคนพร้อมกัน | ทุก request อ่านไฟล์ + แปลง Markdown ใหม่ |
| **ตอนนี้ (มี cache แล้ว)** | **หลักพัน–หมื่นคนพร้อมกัน** | ใช้ `unstable_cache` + Cache-Control API |
| **หลัง Deploy บน Vercel/Cloudflare** | **หลักแสน–ล้านคน** | CDN cache หน้า + API ที่ edge |

---

## 🔴 ปัญหาที่ทำให้ยังรองรับหลักล้านไม่ได้

### 1. ทุก request อ่านไฟล์ + แปลง Markdown ใหม่
- **Home** (`/[lang]`) เรียก `getAllPosts(locale)` ทุกครั้ง → อ่านทุกไฟล์ `.md` + แปลงด้วย remark/rehype
- **API** `/api/posts` เรียก `getAllPosts(locale)` ทุกครั้ง → เหมือนกัน
- **Post** (`/[lang]/post/[id]`) มี `generateStaticParams` → สร้าง static ได้ตอน build แต่ถ้าไม่มี cache ที่ CDN ก็ยังโหลดจาก origin เยอะ

### 2. ไม่มี Cache
- ไม่มี in-memory / Redis cache สำหรับผลลัพธ์ `getAllPosts` / `getPostBySlug`
- ไม่มี `revalidate` (ISR) ให้ Next รู้ว่าให้ cache หน้าไว้กี่วินาที
- API ไม่มี `Cache-Control` → browser/CDN ไม่ cache response

### 3. Single origin
- ถ้า deploy แค่เครื่องเดียว ทุก request ไปที่เครื่องนั้น → CPU / I/O เป็นจุดคอขวด
- หลักล้านคนพร้อมกันต้องพึ่ง **CDN + cache** ให้ request ไปที่ edge มากกว่าไปที่ origin

---

## ✅ แนวทางให้รองรับหลักล้านคน

### ระดับ 1: Cache + ISR (ทำใน Next.js เลย) — ✅ ทำแล้ว

- **Cache ผลการอ่านโพสต์** ✅
  - `lib/markdown.ts`: ใช้ `unstable_cache` กับ `getPostBySlug` และ `getAllPostsCached`
  - Revalidate ทุก 60 วินาที (`CACHE_REVALIDATE = 60`)
  - หน้าโฮมและ API ใช้ `getAllPostsCached(locale)` แทนอ่านไฟล์ทุกครั้ง

- **Cache API** ✅
  - `/api/posts`: เพิ่ม header `Cache-Control: public, s-maxage=60, stale-while-revalidate=300`
  - CDN และ browser สามารถ cache response ได้

### ระดับ 2: Deploy บนแพลตฟอร์มที่มี CDN

- **Vercel / Netlify / Cloudflare Pages**
  - หน้า static/ISR จะถูก cache ที่ edge หลายจุด
  - Traffic หลักล้านส่วนใหญ่จะไปที่ edge ไม่ไปที่ origin
  - รองรับหลักล้านคนได้เมื่อ cache ถูกต้อง

### ระดับ 3: ถ้าจะใหญ่เกินหลักล้าน / มี dynamic เยอะ

- ย้าย content จาก **ไฟล์ Markdown** เป็น **Database** (เช่น PostgreSQL) + **Redis** cache
- ใช้ **Load Balancer** + หลาย instance ของ Next.js
- ใช้ **Edge Runtime** สำหรับ route ที่ต้องตอบเร็วมาก

---

## 🎯 สรุป

- **ก่อนปรับ:** รองรับได้ประมาณ **หลักร้อย–พันคนพร้อมกัน** (แล้วแต่ spec เครื่อง)
- **หลังปรับ (Cache + ISR + Deploy บน Vercel/Cloudflare):** โครงสร้างนี้ **รองรับหลักแสนถึงหลักล้านคน** ได้ เพราะส่วนใหญ่จะถูก serve จาก cache ที่ CDN
- **ถ้าต้องการมั่นใจว่า “รองรับหลักล้านคน” จริงๆ:** ต้องออกแบบ cache + CDN ให้ครอบคลุม (และทดสอบ load) ตามแนวทางด้านบน

---

## ✅ สิ่งที่ implement แล้ว

- **`lib/markdown.ts`**: `getPostBySlug` และ `getAllPostsCached` ใช้ `unstable_cache` (revalidate 60 วินาที)
- **`app/[lang]/page.tsx`**: ใช้ `getAllPostsCached(lang)` สำหรับหน้าโฮม
- **`app/[lang]/post/[id]/page.tsx`**: ใช้ `getAllPostsCached(lang)` สำหรับ related/series
- **`app/api/posts/route.ts`**: ใช้ `getAllPostsCached` + header `Cache-Control` สำหรับ CDN/browser cache

**ขั้นตอนถัดไป:** Deploy บน Vercel หรือ Cloudflare Pages เพื่อให้ CDN cache หน้าและ API ที่ edge → รองรับหลักแสน–ล้านคนได้
